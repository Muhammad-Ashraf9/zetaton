const { createImageInput, imageIdParam } = require("./image/imageValidations");
const validate = require("./validator");

module.exports = {
  createImageInput,
  imageIdParam,
  validate,
};
